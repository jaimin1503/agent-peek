# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Per-developer Claude Code permissions. `.claude/settings.json` is the shared
# project config and stays tracked; only the `.local` half is personal.
.claude/settings.local.json

# Local demo driver for recording the overlay — writes fake session files.
scripts/demo.cjs
